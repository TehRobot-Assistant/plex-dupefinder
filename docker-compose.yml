services:
  plex-dupefinder:
    image: tehrobot/plex-dupefinder:latest
    container_name: plex-dupefinder
    ports:
      - "3000:3000"
    volumes:
      - ./config:/config
    environment:
      - PORT=3000
      - CONFIG_PATH=/config
    restart: unless-stopped
